{
  "private": true,
  "name": "encrypted-notes-dapp",
  "version": "0.2.0",
  "keywords": [
    "Internet Computer",
    "Motoko",
    "Svelte",
    "Canister",
    "Docker",
    "Rust"
  ],
  "scripts": {
    "dfx:start": "sh dfx-start.sh",
    "generate": "dfx generate",
    "dev:vetkd": "cd vetkd && npm run dev",
    "dev:payment": "cd payment && npm run start",
    "build:vetkd": "cd vetkd && npm install && npm run build",
    "build:payment": "cd payment && npm install && npm run build",
    "deploy": "cargo test && dfx deploy",
    "test": "cargo test"
  },
  "workspaces": [
    "vetkd",
    "payment"
  ]
}